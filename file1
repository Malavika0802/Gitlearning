hai this is git folder
there is basically 3 command
git init
git status
git add -A 
git commit -m"commit message"

now we need to add it to remote repository 
git remote add remote_name remote_path(for adding remote)
git push remote_name branch_name(master/build)


now we added this to git hub